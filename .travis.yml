language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"

install:
  - pip install -e .[test]

before_script:
  - flake8 gengo

script:
  - nosetests --rednose --with-coverage --cover-package=gengo

notifications:
  irc: "irc.freenode.net#Gengo"

language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6

install:
  - pip install -e .[test]

script:
  - flake8 gengo
  - nosetests --rednose --with-coverage --cover-package=gengo

notifications:
  irc: "irc.freenode.net#Gengo"
